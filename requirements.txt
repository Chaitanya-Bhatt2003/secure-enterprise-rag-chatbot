# --- Core app ---
streamlit>=1.36
python-dotenv>=1.0

# --- LangChain + Groq LLM ---
langchain>=0.2
langchain-community>=0.2
langchain-groq>=0.1.6
langchain-text-splitters>=0.2

# --- Vector store + embeddings ---
chromadb>=0.5
sentence-transformers>=3.0

# --- Document loaders ---
pypdf>=4.0
docx2txt>=0.8

# --- Hybrid search (BM25) ---
rank-bm25>=0.2.2

# --- PII masking (optional but recommended).
# masking.py falls back to regex-based detection automatically if these
# fail to import, so the app still runs without them.
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
